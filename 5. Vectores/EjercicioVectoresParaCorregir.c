/*

 COPIAR ESTE ARCHIVO EN FORMA LOCAL Y CORREGIRLO.

*/

#define TAM 10

int fBuscar(int,int,int);
void fCarga(int , int);
void fMostrar(int.int);

int main{}
{
  int va[TAM],vb[TAM]={0},valor,pos;


  fCarga(va[],TAM);


  printf("Ingrese valor");
  scanf("%d",&valor);

  while(valor>0)
  {

      pos=fBuscar(va,valor,TAM);
      if(pos!=-1)
         vb[]++;

     printf("Ingrese valor");
     scanf("%d",&valor);

  }

fMostrar(vb,TAM);

 gecth();
 return 0;

}

int fBuscar(int va,int dato,int n)
{
    int i=0;

    while(pos==-1 && va == dato)
          if(va[i]==dato)
             pos=i
          else
            i=i+;
   return pos;

}
